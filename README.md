# bambu-monitor
Web interface for monitoring Bambu Lab printers

Copy `config/config.json` into `config/config.local.json`, and then populate it with a list of printers

```
npm install
node src/server.js
```

Browser access:
```
http://host:3000
```
